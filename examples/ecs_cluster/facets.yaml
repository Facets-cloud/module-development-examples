intent: ecs_cluster
flavor: rohit-ecs-cluster
clouds:
  - aws

version: 1.0

spec:
  type: object
  properties: {}
  required: []

outputs:
  default:
    type: "@output/ecs_cluster_details"

sample:
  kind: ecs_cluster
  flavor: rohit-ecs-cluster
  version: "1.0"
  spec: {}
